#ifndef IMMOVABLE_HPP
#define IMMOVABLE_HPP

struct Immovable {
  Immovable(double const in_pos_x, double const in_pos_y, double const in_radius);
  double pos_x;
  double pos_y;
  double radius;  
};

#endif
